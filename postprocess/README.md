# Postprocess Module

This module owns mask-level postprocessing that happens after segmentation.

Current implementation runs natively here with code migrated from
`archive/legacy_modules/segmentation_multiscale_cellpose_3D/postprocessing/`
(frozen reference copy).
