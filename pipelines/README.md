# Pipelines Module

This folder holds end-to-end orchestrators that call sibling modules:
- `segmentation`
- `postprocess`
- `features`
- `qc`
- `visualization`

Business logic should remain in module folders, not here.
